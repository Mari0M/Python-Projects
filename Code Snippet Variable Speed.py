x = input("What is the value of x? ")

if int(x) > 100:
    y = 20
    z = 40
    print("Y is now: " + str(y))
    print("Z is now: " + str(z))


else:

    print("Invalid option! Goodbye!")

a = input("What is the value of a? ")

if int(a) < 10:
    b = 0
    c = 1
    print("b is now: " + str(b))
    print("c is now: " + str(c))


else:

    print("Invalid option! Goodbye!")